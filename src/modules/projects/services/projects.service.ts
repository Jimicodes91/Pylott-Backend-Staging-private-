import { injectable } from 'tsyringe';

// switch project
// change stage
// change milestone remember a stage is associated with a project
@injectable()
export class ProjectService {
  private traceId = '[Project Service]';
}
